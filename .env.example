# Expo client (web/Android/iOS)
#
# Runtime configuration belongs in the app's encrypted Settings and Config
# Studio. The client has no required build-time URL, token, provider, or domain.
# Use `npm run android` for a bundled release APK. Keep this file secret-free.
