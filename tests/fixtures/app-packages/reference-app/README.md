# reference-app

Lane C runtime proof fixtures.

- `compiled/` is the executable Day 1 package input.
- `authoring/` is placeholder-only for a future compiler.
- `fixtures/records.json` seeds renderer tests.
