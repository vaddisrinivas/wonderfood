import { ManifestMobileSurface } from '@/src/domain/manifest-mobile-surface';

/**
 * Route shell only. The active domain manifest owns all Food presentation.
 */
export default function FoodScreen() {
  return <ManifestMobileSurface />;
}
