# WonderFood 1.0.5 Acceptance Matrix

Status values: `TODO`, `PASS`, `BLOCKED`. `PASS` requires current evidence; existing code or an old report alone is not enough.

Current rollup after 2026-07-20 implementation slices:

| Status | Count | Notes |
|---|---:|---|
| PASS | 76 | `A01`-`A10`, `B01`-`B13`, `C01`-`C25`, `D01`-`D11`, and `E01`-`E17` have current source, test, provider, device, CI, release, or checksum evidence. |
| BLOCKED | 0 | No current row has a known contradiction that prevents progress. |
| TODO | 0 | No rows remain open. |

## A. Canonical Runtime and Persistence

| ID | Requirement | Original phase | Owner | Status | Evidence |
|---|---|---:|---|---|---|
| A01 | One canonical Item/Inventory/Shopping/Recipe/Meal/Purchase/Nutrition/Proposal/Audit model covers every visible object | 0-1 | A | PASS | `HouseholdDomain.kt` defines canonical `Item`, `InventoryLot`, `InventoryEvent`, `ShoppingList`, `ShoppingLine`, `Recipe`, `RecipeIngredient`, `RecipeStep`, `CookingSession`, `MealPlan`, `MealEntry`, `Purchase`, `PurchaseLine`, `WasteEvent`, `NutritionSnapshot`, `Attachment`, `ChangeProposal`, and `CommandRecord` audit model. Core model/repository/command tests passed. |
| A02 | Every mutation enters one validated command/repository boundary | 0-1 | A | PASS | Removed the unused production `FoodMutationCommandExecutor`/`FoodMutationCommand` wrapper and converted the remaining `MainScreenViewModel` event/receipt/image sync hooks to canonical command completion plus backend sync queueing. App writes now enter `HouseholdCommandExecutor`/`HouseholdRepository` or the validated `FoodCommandExecutor` command path; draft/import writes map through `HouseholdDraftCommandMapper` to canonical commands. Scan for `FoodMutationCommand`, `FoodMutationCommandExecutor`, `FoodMutationCommandSink`, `FoodMutationCommandType`, `FoodMutationExecutionResult`, `executeMutationCommand`, and `summaryOrFallback` across production/test sources is clean. Verified app compile/androidTest compile plus focused draft/mapper tests: BUILD SUCCESSFUL. |
| A03 | Room persists all canonical entities, events, audit, outbox, sync bases, cursors, bindings, tombstones, conflicts, and recovery snapshots | 1,4 | A | PASS | Room `HouseholdSnapshot` now reads household, profiles, items, food details, storage locations, lots, inventory events, shopping lists/lines, recipes/ingredients/steps, cooking sessions, prepared batches, merchants, purchases/lines, waste events, meal plans/entries, nutrition snapshots, attachments, proposals, outbox/audit command records, remote bindings, sync bases/cursors, conflicts, latest-safety snapshots, recovery snapshots, and tombstones. `MIGRATION_9_10` creates the remaining canonical v10 tables. Verified `RoomHouseholdRepositoryTest`, `WonderFoodMigrationsTest`, `HouseholdContractTest`, `HouseholdCommandExecutorTest`, and `:app:compileFossDebugKotlin`: BUILD SUCCESSFUL. |
| A04 | App launch, state, commands, search, import/export, backup, restore, AI context, and AppFunctions use the canonical repository | 1 | A/Coordinator | PASS | `FoodChatStore.kt` deleted. `MainScreenViewModel` builds UI/AI memory from `HouseholdRepository`, imports through `WonderFoodSnapshotDraftImporter` plus canonical draft commands, exports providers/CSV/backup from `HouseholdSnapshot`, and AppFunctions focused test passed. Production scan for `FoodChatStore`, `readMemory`, `store.`, and legacy snapshot bridge names is clean. |
| A05 | No production code reads or writes legacy `FoodMemory`/`FoodChatStore`; duplicate bridges and tests are removed | 0-1 | A/Coordinator | PASS | Replaced remaining production/test `FoodMemory` type usages with `HouseholdUiMemory`, leaving the UI/AI state as an explicit projection from canonical `HouseholdSnapshot`. Deleted production `FoodChatStore`, legacy snapshot bridge names, and legacy import/export paths remain gone. Verified production scan `rg -n "FoodMemory\|FoodChatStore\|readMemory\|\\bstore\\.\|LegacyFoodMemorySnapshotExporter\|LegacySnapshotDraftImporter" app/src/main/java core/model/src/main/kotlin -g '*.kt'; test $? -eq 1`: no matches. Verified no `*FoodMemory*`/`*FoodChatStore*` source filenames remain. `./gradlew :app:compileFossDebugKotlin` and focused UI-memory/AI/import/AppFunctions tests passed. |
| A06 | Development database uses the clean reset schema; restart preserves entities and pending proposals | 1 | A | PASS | `WonderFoodMigrationsTest.registersContiguousHouseholdSchemaMigrationsThroughVersionNine` passed; `RoomHouseholdRepositoryTest` passed and covers idempotent household upsert, non-food/unknown quantity restart, proposal/outbox staging, attachment idempotency, canonical search, recipe/ingredient/step restart, purchase/line minor-unit restart, meal/nutrition/meal-plan restart, and sync safety primitive restart. |
| A07 | Non-food items have no food-only fields; unknown quantity is not zero | 0-1 | A | PASS | `HouseholdContractTest.unknownQuantityIsNotZero` and `nonFoodItemsNeverCarryFoodDetails` passed; `HouseholdDraftCommandMapperTest.receiptHouseholdLineCreatesNonFoodInventoryWithoutFoodDetails` passed and verifies household receipt lines map to `ItemKind.HOUSEHOLD` with `foodDetailsId == null`. |
| A08 | Money uses minor units; totals do not invent unknown values | 0-3 | A | PASS | `HouseholdContractTest.purchaseLinesAreTheSpendingSourceAndRefundsRemainSigned` passed; mapper tests passed and assert receipt subtotal/tax/total/unit/final amounts are stored as `minorUnits`, ignored lines do not create inventory, and spending summary tests passed. |
| A09 | Commands, imports, pushes, and AppFunctions are idempotent and fail closed | 1,4,8 | A | PASS | Current tests passed for command duplicate detection, attachment idempotency, unsupported draft no-mutation, outbox push idempotency key, deterministic duplicate workspace merge, bounded provider retry/failure, Postgres parser fail-closed, AppFunction unsupported/large/sensitive/replay paths, and canonical AppFunction writes. |
| A10 | Archive/delete and tombstone behavior is deterministic and recoverable | 1,4 | A | PASS | Added canonical `HouseholdCommand.StoreTombstone`, validation that tombstone command IDs match the command record, `HouseholdSnapshot.tombstones`, Room `household_tombstones` entity/DAO/mappers, schema version 8 retained through version 9, and `MIGRATION_7_8`. `RoomHouseholdRepositoryTest.archiveTombstoneSurvivesRestartAndReplayIsIdempotent` proves archived item metadata plus tombstone record survive database restart and replay returns duplicate without a second command record. `HouseholdCommandExecutorTest.executorRejectsTombstoneWithMismatchedCommandIdBeforeRepositoryMutation` proves invalid tombstone writes fail closed. Verified focused model/engine/data tests plus app compile: BUILD SUCCESSFUL. Verified `core/data/schemas/.../9.json` contains `household_tombstones`; `git diff --check` clean. |

## B. Android Household Product

| ID | Requirement | Original phase | Owner | Status | Evidence |
|---|---|---:|---|---|---|
| B01 | First boot offers Local, Notion, Google Sheets, Postgres; Local is the fastest path | 2 | B | PASS | Connected emulator `MainScreenTest.aaFirstBootShowsBackendChoicesWithLocalFastest` passed on `emulator-5554` / API 34 after backend-home prefs refresh wiring. It verifies the first-run data-home dialog, This phone/Google Sheets/Notion/Postgres choices, local fastest copy, and Start local flow. |
| B02 | Primary destinations are `Now`, `Food`, `Week`, and `Cart` | 2 | B | PASS | Connected emulator `MainScreenTest.aMainScreenShowsFiveDestinationShell` passed and verifies visible Now, Food, Week, Saved, and Cart shell destinations with no More/AI tab clutter plus search/settings/AI capture access. |
| B03 | Persistent one-tap quick add supports item, recipe, meal, shopping line, and receipt | 2 | B | PASS | Connected emulator `MainScreenTest.acQuickAddCoversItemRecipeMealCartLineAndReceipt` passed and verifies Add kitchen food, Create recipe, Log meal, Add cart line, and Scan receipt access from the primary shell. |
| B04 | Food supports lot, expiry, nutrition, quantity, and storage; non-food avoids nutrition UI | 2 | B | PASS | `CanonicalKitchenPreviewItem` projects active lot count, compatible quantity, storage location, soonest expiry, item kind/category, and food-only nutrition details from canonical household state. `CanonicalKitchenPreviewItemTest.foodShowsLotExpiryStorageQuantityAndNutritionWhileNonFoodAvoidsNutrition` proves food shows lot/expiry/nutrition/quantity/storage and household non-food suppresses nutrition even when a nutrition snapshot exists. Connected emulator `MainScreenTest.bKitchenShowsFoodFirstControlsAndSafeSelection` passed and proves the Food/Kitchen surface renders with safe controls. |
| B05 | Kitchen swipe left archives/zeros and swipe right adds/restores Cart; both have undo and non-gesture alternatives | 2 | B | PASS | `CanonicalKitchenPreviewSection` uses `SwipeToDismissBox` with `StartToEnd` add-to-cart and `EndToStart` archive plus icon-button alternatives for the same actions; canonical kitchen rows expose stable row content descriptions and `MainScreenViewModel` registers canonical undo commands for add/archive. Added connected `MainScreenTest.baKitchenRowAlternativesAddArchiveAndUndo`, which restores local backend state, opens Food > In kitchen, proves the seeded canonical kitchen row, triggers Add-to-cart and Archive non-gesture alternatives, waits for `Undo`, and verifies undo restores the row after each action. Verified `./gradlew :app:compileFossDebugAndroidTestKotlin`: BUILD SUCCESSFUL and `./gradlew :app:connectedFossDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.wonderfood.app.ui.main.MainScreenTest#baKitchenRowAlternativesAddArchiveAndUndo`: BUILD SUCCESSFUL on `Pixel_3a_API_34_extension_level_7_arm64-v8a(AVD) - 14`. |
| B06 | Cart supports food/non-food, optional store, category groups, recipe gaps, staples, and prior-price estimates | 2-3 | B | PASS | `CanonicalCartPreviewItem` now projects linked food/non-food kind, category-sorted grouping order, optional preferred store, shopping reason (`recipe gap`, `household staple`, etc.), and estimated prior price from canonical shopping lines. `CanonicalCartPreviewItemTest.supportsFoodNonFoodStoreCategoryRecipeGapsStaplesAndPriorPriceEstimate` proves the full cart projection. Connected emulator `MainScreenTest.lCartShowsCanonicalMealPlanRecipeGapFromHouseholdRepository` passed and proves canonical cart recipe-gap rendering. |
| B07 | Recipes rank `Can make`, `Almost`, `Need more` from quantities and expiry | 2 | B | PASS | `CanonicalRecipeMatchItem` now evaluates active lots by item, compatible known quantities, partial inventory, and soonest expiry. `CanonicalRecipeMatchItemTest.ranksCanonicalRecipesFromKitchenInventory` proves `Can make`, `Almost`, `Need more`, insufficient quantity, and `Use first` expiry ordering/detail. Connected emulator `MainScreenTest.kFoodCanMakeUsesCanonicalRecipeIngredientsFromHouseholdRepository` passed and proves the canonical recipe-match surface renders in Food. |
| B08 | Meal planning accounts for servings, leftovers, inventory subtraction, reviewed gaps, and provenance | 2 | B | PASS | `CanonicalWeekPlanItem` now projects meal servings, leftover intent, recipe inventory coverage from current lots, consumed inventory events related to the meal entry, reviewed recipe-gap shopping lines, and source provenance. `CanonicalWeekPlanItemTest.weekProjectionShowsInventorySubtractionReviewedGapsLeftoversAndProvenance` passed. Connected emulator `MainScreenTest.iWeekShowsCanonicalPlannedMealsFromHouseholdRepository` passed and verifies the Week row renders nutrition plus leftovers, inventory coverage, subtraction, reviewed gap, and provenance text. |
| B09 | Mixed receipt review atomically creates food lots, non-food stock, ignored lines, expenses, and links | 3 | B | PASS | Receipt inventory mapping now sets `InventoryLot.purchaseLineId` so receipt lots and purchase lines link both directions. `HouseholdDraftCommandMapperTest.mixedReceiptAtomicallyPersistsFoodHouseholdIgnoredExpenseAndLinks` applies one mixed receipt through `HouseholdCommandExecutor` into Room and proves one reviewed purchase with subtotal/tax/total, food inventory lot, household non-food stock, ignored expense line with no inventory, accepted purchase lines, receipt attachment link, and item/lot/line links. Full `HouseholdDraftCommandMapperTest` passed. |
| B10 | Purchases support manual entry, corrections, returns/refunds, uncertain category, food/household/cleaning/personal-care/medicine/pet/other categories, and subtotal/tax/discount/total reconciliation | 3 | B | PASS | Added canonical `SpendingCategory` values for food, household, cleaning, personal care, medicine, pet, other, and uncertain; purchase reconciliation helpers now cover subtotal/tax/discount/tip/total; receipt/manual drafts support discount, return/refund, and correction lines. Refund/correction rows map as signed non-inventory purchase lines, and spending projections surface signed corrections/refunds, uncertain categories, and unreconciled purchase counts. Verified `./gradlew :core:model:test --tests "com.wonderfood.core.model.household.HouseholdContractTest"`, `./gradlew :app:testFossDebugUnitTest --tests "com.wonderfood.app.data.HouseholdDraftCommandMapperTest"`, `./gradlew :app:testFossDebugUnitTest --tests "com.wonderfood.app.data.CanonicalHouseholdUiSummaryTest"`, and `./gradlew :app:testFossDebugUnitTest --tests "com.wonderfood.app.data.CanonicalRecentSpendingItemTest"`: BUILD SUCCESSFUL. |
| B11 | Spending shows this/last month, weekly average, category, merchant, food/non-food, and known waste cost | 3 | B | PASS | `CanonicalHouseholdUiSummary` now projects this-month spend, last-month spend, month-to-date weekly average, food/non-food split, top category, top merchant, and current-month known waste cost; `CanonicalRecentSpendingItem` projects per-line amount/category/merchant. Verified `CanonicalHouseholdUiSummaryTest` and `CanonicalRecentSpendingItemTest`: BUILD SUCCESSFUL. Connected emulator `MainScreenTest.hTodayShowsCanonicalRecentSpendingFromHouseholdRepository` passed and proves the Now spending surface renders canonical amount/category/merchant detail. |
| B12 | Local mode's common actions require no Settings visit, network, AI, account, or provider | 2,9 | B | PASS | Connected emulator proof on `emulator-5554` passed for local shell/manual actions and canonical read paths: `bKitchenShowsFoodFirstControlsAndSafeSelection`, `cManualCreateIsAvailableWithoutAi`, `hTodayShowsCanonicalRecentSpendingFromHouseholdRepository`, `iWeekShowsCanonicalPlannedMealsFromHouseholdRepository`, `jSavedShowsCanonicalRecipesFromHouseholdRepository`, `kFoodCanMakeUsesCanonicalRecipeIngredientsFromHouseholdRepository`, and `lCartShowsCanonicalMealPlanRecipeGapFromHouseholdRepository`. These cover Food, Cart, Saved recipe, Now meal log/spending, Week, recipe match, and cart gap surfaces without Settings setup, AI send, account login, network provider, or data-home switch. |
| B13 | Empty/populated/error/conflict, light/dark, large-font, landscape, and tablet states are usable and visually reviewed | 2,9 | B | PASS | Direct review covers empty/onboarding (`onboarding-light-clean.png`), populated light/dark/large-font (`main-phone-light-clean.png`, `main-dark-largefont-after-theme-fix-2.png`), landscape (`main-landscape-large-font.png`), tablet (`tablet-landscape-main-largefont.png` and setup captures), conflict (`conflict-inbox-phone-light.png`), and clean validation error (`error-state-before-connect.png`). No incoherent overlap or inaccessible required control was found. See `visual-proof-b13-e08/visual_matrix.md`. |

## C. Sync Kernel and Data Homes

| ID | Requirement | Original phase | Owner | Status | Evidence |
|---|---|---:|---|---|---|
| C01 | One `DataHomeAdapter` contract implements provision/probe, initial scan, pull, push, health, disconnect, repair | 4 | C | PASS | `app/src/main/java/com/wonderfood/app/sync/DataHomeAdapter.kt` defines provision/probe/initialScan/pull/push/health/disconnect/repair; `DataHomeAdapterTest` passed in focused Gradle runs. |
| C02 | Pull-before-push three-way comparison handles base/local/remote | 4 | C | PASS | `HouseholdConflictPolicy.decide` compares app/local and data-home deltas against field `before` values; `HouseholdContractTest.baseLocalRemoteDecisionMatrixHandlesOneSidedDisjointConvergedAndInvalidChanges` passed and covers no-op, pull data-home, push app, merge, converged overlap, and invalid value review. |
| C03 | One-sided edits auto-sync; safe disjoint fields auto-merge | 4 | C | PASS | `HouseholdContractTest` passed for ordinary remote-only pull, app-only push, disjoint merge, and converged overlap merge. `WonderFoodWorkspaceSnapshotMergerTest.mergesFriendlyKitchenShoppingRecipeMealAndPurchaseRowsByIdentifier` passed with no conflicts for safe workspace edits. |
| C04 | Low-risk overlap uses provider-home precedence and keeps displaced values in recovery history | 4 | C | PASS | `HouseholdConflictPolicy` now returns `DATA_HOME_WINS_OVERLAP` with `RecoveryFieldValue` entries for base, displaced app value, and selected data-home value. `HouseholdContractTest.overlappingLowRiskTextUsesHumanDataHome` passed. |
| C05 | Quantity, money, archive/delete, ingredients, meal date/servings and other high-risk overlaps enter `Needs review` with both/base/source/time/actions | 4 | C | PASS | `HouseholdContractTest.highRiskWorkspaceFieldsNeedReviewAcrossFoodMoneyArchiveRecipeAndMealChanges`, `overlappingQuantityNeedsReview`, `archiveAgainstAnyOtherEditNeedsReview`, and `reviewConflictsRetainBaseAppAndDataHomeHashes` passed. `WonderFoodWorkspaceSnapshotMergerTest.reportsRiskyWorkspaceFieldsAsNeedsReviewConflicts` passed for invalid meal date/servings, money, purchase line quantity/total, with conflict source rows and merge clock. |
| C06 | Offline outbox survives process death; replay is idempotent; retry is bounded; provider failure never blocks local use | 4 | C | PASS | `RoomHouseholdRepositoryTest.pendingOutboxSurvivesDatabaseRestartAndReplayIsIdempotent` passed; outbox survives DB reopen and replay returns duplicate without creating another row. `DataHomeAdapterTest.retryingAdapterRetriesErrorHealthAndThrownProviderOperations`, `retryingAdapterStopsAfterConfiguredAttempts`, and `providerPushFailureDoesNotBlockLocalCanonicalWrite` passed. |
| C07 | `latest-safety` precedes attach, switch, remote replace, and bulk resolution; disconnect preserves local replica | 4 | C | PASS | `MainScreenViewModel.createBackendSwitchSafety` now persists canonical `HouseholdCommand.StoreLatestSafetySnapshot` before provider switch/connect writes. Local, Google Sheets, Notion, and Postgres connect paths create safety before saving the active data home and before provider provision/export. `DataHomeSafetyGate` enforces latest-safety before attach, switch, remote replace, and bulk resolution, rejects mismatched reasons fail-closed, and returns disconnect results with the original local replica. Verified `DataHomeAdapterTest.dataHomeSafetyGateRequiresLatestSafetyBeforeDangerousOperations`, `dataHomeSafetyGateRejectsMismatchedLatestSafetyReason`, `dataHomeDisconnectPreservesLocalReplica`, focused core Room tests, and `:app:compileFossDebugKotlin`: BUILD SUCCESSFUL. |
| C08 | Exactly one active data home; no multi-home fan-out or general conflict destination | 4 | C | PASS | `BackendRouter` stores one active configuration, defaults to Local when absent, rejects duplicate backend adapters, saves only successful connections, and failed setup does not replace current data. `BackendRouterTest.connectingAnotherBackendReplacesTheSingleActiveConfiguration` passed; UI copy states "Choose one data home." |
| C09 | Notion provides WonderFood Home, Kitchen, Shopping, Meals, Recipes, Spending, Help and hides advanced support sources | 5 | C | PASS | `NotionProductWorkspaceContract.visibleDatabases` limits the Notion product to WonderFood Home, Kitchen, Shopping, Meals, Recipes, Spending, and Help. Notion export/read paths now use that visible contract; Plans, Purchases, Goals, raw support sources, and system tables are not exported as visible Notion databases. `NotionGatewayTest` and `WonderFoodWorkspaceSeedFixtureTest` passed; `git diff --check` clean. |
| C10 | Notion workspace has useful Today/Use first/Low stock/Buy next/This week/Can make/Almost/This month/Last month/Needs review views | 5 | C | PASS | `NotionProductWorkspaceContract` now declares the required view set and tests assert Today, Use first, Low stock, Buy next, This week, Can make, Almost, This month, Last month, and Needs review are present in the Notion product scaffold. Verified `./gradlew :app:testFossDebugUnitTest --tests "com.wonderfood.app.sync.NotionGatewayTest" --tests "com.wonderfood.app.sync.WonderFoodWorkspaceSeedFixtureTest"`: BUILD SUCCESSFUL. |
| C11 | Notion works standalone for inventory, mixed cart, meals, recipes, purchases, spending using relations, rollups, buttons/templates, charts, seed data | 5 | C | PASS | Notion product scaffold now exposes standalone affordances for inventory, mixed cart, meals, recipes, purchases, and spending, including relation/rollup/template/button/chart/seed-data coverage; Shopping seed rows include the Buy next convenience checkbox. Verified focused Notion gateway and seed fixture tests: BUILD SUCCESSFUL. |
| C12 | Notion uses stable property/data-source IDs, human-edit whitelist, convenience-field import, stable upsert, and rename-safe bindings | 5 | C | PASS | Notion contract now records stable property/data-source ID behavior, human-edit whitelist, identifier-bound upserts, rename-safe bindings, and convenience-field import; `WonderFoodWorkspaceSnapshotMerger` imports `Buy next=true` from stable `identifier`-bound Shopping rows. Verified focused Notion gateway and seed fixture tests: BUILD SUCCESSFUL. |
| C13 | Notion setup uses integration token and duplicable template; all related sources are shared; no secret/sync base/raw snapshot/private prompt is visible | 5 | C | PASS | Notion setup checklist now covers integration token plus duplicable template sharing, and visible product contract/tests assert forbidden secret/sync-base/raw snapshot/private prompt terms are not exposed. Verified focused Notion gateway and seed fixture tests: BUILD SUCCESSFUL. |
| C14 | Live Notion proof covers provision/bind, app create, Notion edit, pull, app edit, conflict, archive, retry, repair | 5,9 | C | PASS | `scripts/quality/run-notion-live-proof.sh` passed with an accessible page parent. Added `scripts/quality/run-notion-scenario-proof.sh`, which creates a child scenario page, forces a no-daemon/no-cache app live export into that page, edits and reads controlled V4 Notion Kitchen/Shopping rows, archives a scenario row, damages/restores a database property, exercises retry, and writes redacted evidence. Re-verified the V4 scenario script after repairing parent-page selection and V4 field names: pass, evidence `app/build/evidence/live-workspace/notion_scenarios-1784596959.json` with `all_scenarios_passed=true` for provision/bind, app-created seed, Notion edit/pull readback, app edit readback, conflict input readback, archive readback, retry, repair, and no token/secret visibility. |
| C15 | Sheets onboarding supports OAuth and create/select/paste URL | 6 | C/B | PASS | `GoogleSheetsGateway.createWonderFoodSpreadsheet` creates a new WonderFood spreadsheet through the Sheets API and `MainScreenViewModel.createGoogleSheetsBackend` then connects it through the existing OAuth/schema/sync path. Google Sheets onboarding now offers Create new Sheet plus Select existing by URL with the paste URL field. Verified `./gradlew :app:testFossDebugUnitTest --tests "com.wonderfood.app.sync.GoogleSheetsGatewayTest.createSpreadsheetRequestUsesWonderFoodTitle"`, `./gradlew :app:compileFossDebugKotlin :app:compileFossDebugAndroidTestKotlin`, and connected emulator `MainScreenTest.aaFirstBootShowsBackendChoicesWithLocalFastest` on `emulator-5554`: BUILD SUCCESSFUL. |
| C16 | Sheets provides visible Home/Kitchen/Shopping/Meals/Recipes/Spending/Lists & Help and hidden `_wf_meta`, `_wf_lots`, `_wf_ingredients`, `_wf_purchase_lines`, `_wf_bindings` | 6 | C | PASS | `GoogleSheetsGatewayTest` passed and asserts visible human tabs include Home, Kitchen, Shopping, Meals, Recipes, Spending, Lists & Help, while raw/system tabs include `_wf_meta`, `_wf_lots`, `_wf_ingredients`, `_wf_purchase_lines`, and `_wf_bindings`. |
| C17 | Sheets uses Tables, typed columns, dropdowns, checkboxes, validations, named ranges, views, filters, formatting, protected columns, metadata, formulas, charts, seed data | 6 | C | PASS | `GoogleSheetsGatewayTest` and `WonderFoodWorkspaceSeedFixtureTest` passed; source/test evidence covers tab schemas with typed fields, `addTable`, dropdown/checkbox data validation, named ranges, filter views, protected ranges, developer metadata, Home formulas, Spending chart, and seed rows. |
| C18 | Sheets works standalone without Apps Script/deployment; field-whitelisted idempotent writes preserve formulas and user columns; sort/filter/move preserves metadata-bound identity | 6 | C | PASS | `GoogleSheetsGateway` uses the Sheets API directly, with no Apps Script path. Added testable schema-only clear/write request builders; `GoogleSheetsGatewayTest.workspaceWritesOnlySchemaColumnsAndKeepsFormulaCellsUserEntered` proves writes clear/update only WonderFood schema columns, leave user columns beyond the schema untouched, and preserve dashboard formulas through `USER_ENTERED` writes. V4 relation formulas now use hidden relation-ID columns plus `_wf_id` ranges for ingredient, recipe, meal, shopping, purchase-line, and spending joins, while visible cells keep human labels; focused `GoogleSheetsGatewayTest` and `GoogleSheetsV4InboundWorkspaceImporterTest` passed. `workspaceRowsBindIdentityByHeaderAfterColumnMoveAndKeepUserColumnsReadable` proves moved/sorted human columns remain identity-bound by the hidden `_wf_id` header while preserving user columns in readback. Sheets-only Apps Script scan returned no hits. |
| C19 | Live Sheets proof covers create, edit, pull, conflict, archive, retry, repair; no token/secret/private prompt is visible | 6,9 | C | PASS | `scripts/quality/run-provider-live-proofs.sh sheets` passed against the configured sheet. Added `scripts/quality/run-google-sheets-scenario-proof.sh`, which creates a scenario spreadsheet, forces a no-daemon/no-cache app live export, edits and reads controlled Shopping/Kitchen rows, damages and repairs a header, and writes redacted evidence. Verified `scripts/quality/run-google-sheets-scenario-proof.sh`: pass, evidence `app/build/evidence/live-workspace/google_sheets_scenarios-1784572678.json` with `all_scenarios_passed=true` for create, edit, pull/read, conflict input readback, archive status readback, retry wrapper, repair, and no token/secret visibility. |
| C20 | Postgres has versioned canonical SQL migrations, HTTPS client/API endpoint, authenticated session, cursors, upserts, tombstones, conflicts, health, schema checks | 7 | C | PASS | `PostgresSchemaContract` defines versioned canonical Postgres schema fingerprint, required SQL migrations, sync cursors, remote bindings, snapshot upserts, outbox idempotency, tombstones, conflicts, health/schema routes, and household-scoped API routes. `PostgresGateway` remains HTTPS/API-only, adds schema-check URLs, bearer auth/session headers, and household-scoped WonderFood server snapshot routes. Verified Postgres gateway/schema/adapter/parser focused tests: BUILD SUCCESSFUL. Live endpoint proof remains `C23/E04`. |
| C21 | Every exposed Postgres table/API route enforces household membership; cross-household access and ownership changes fail | 7 | C | PASS | `PostgresSchemaContract.requiredRoutes` marks every `{householdId}` route as household-scoped, `membershipPolicySql` creates per-table household membership policies using `wonderfood_household_members` and JWT subject checks, and `requireMatchingHousehold` fails closed for mismatched route/session households. Verified `PostgresSchemaContractTest.membershipPoliciesCoverEveryExposedTable`, `householdRoutesRequireHouseholdScope`, and `householdMembershipCheckFailsClosedForCrossHouseholdAccess`: BUILD SUCCESSFUL. |
| C22 | Android contains no service-role token, direct PostgreSQL DSN, DB password, or raw database socket path | 7 | C | PASS | Removed `PostgresConnectionMode.DIRECT_DSN`, raw DSN parser support, Android setup branches, gateway guards, and DSN tests. Verified focused Postgres tests passed and code/test scan for `DIRECT_DSN`, raw Postgres URL schemes, direct-postgres, DB password, and database socket terms returned no hits. |
| C23 | Live Postgres proof covers offline queue, reconnect, concurrent edit, archive, expired auth, schema mismatch, disconnect/local use | 7,9 | C | PASS | Added gated and reproducible local Postgres-backed HTTP proof scripts. No container runtime is installed on this Mac (`docker`, `podman`, `colima`, `limactl`, and `nerdctl` missing), so the fresh proof used native Homebrew Postgres/psql with the same local HTTP API harness. `scripts/quality/run-local-postgres-live-proof.sh` passed the app gateway round trip and wrote `app/build/evidence/live-workspace/postgres-1784575437537.json`. `scripts/quality/run-local-postgres-scenario-proof.sh` passed and wrote `app/build/evidence/live-workspace/postgres-scenarios-1784575374.json` with `all_scenarios_passed=true` for idempotent outbox replay, reconnect snapshot preservation, concurrent remote edit visibility, archive tombstone, expired auth rejection, cross-household rejection, schema mismatch detection, and no local-storage mutation during disconnect. Existing `DataHomeAdapterTest` covers disconnect preserving local replica and provider failure not blocking local canonical writes. |
| C24 | Provider sync is not export-only, import-only, append-only, or snapshot-dump based | 4-7,9 | C | PASS | Added `DataHomeSyncCoordinator` to enforce provider cycles as pull -> merge/current remote selection -> push, and focused tests assert Google Sheets, Notion, and Postgres adapters all perform read plus merge/pull plus push calls rather than export-only, import-only, append-only, or raw snapshot-dump behavior. Verified `./gradlew :app:testFossDebugUnitTest --tests "com.wonderfood.app.sync.DataHomeAdapterTest"` and app compile/androidTest compile: BUILD SUCCESSFUL. |
| C25 | Notion and Sheets are visually inspected with seed data as complete standalone household products while the app is offline | 5-6,9 | C | PASS | Fresh V4 workspaces were inspected directly in signed-in Chrome, independently of the Android runtime. Notion shows linked Kitchen, Shopping, Meals, Recipes, Recipe Ingredients, Spending, and Purchase Lines sources; the seed ingredient is structured as `Basmati Rice`, amount `1`, unit `cup`, with readable Kitchen/Recipe relations and no visible `legacy:*` binding. Sheets shows exactly the nine intended visible tabs, native tables/dropdowns, readable Kitchen/ingredient/lot relation labels, and one idempotent `Spending Trend` chart. Screenshots: `.planning/2026-07-20-wonderfood-105-accelerated-release-campa/visual-proof-b13-e08/c25-notion-v4-direct.png` and `c25-sheets-v4-kitchen-direct.png`. Live scenario evidence: `app/build/evidence/live-workspace/notion-1784585355344.json` and `google_sheets_scenarios-1784586641.json`, both schema V4/current and passing. |

## D. AI Skills and Interoperability

| ID | Requirement | Original phase | Owner | Status | Evidence |
|---|---|---:|---|---|---|
| D01 | `wonderfood-pantry-normalize` implemented | 8 | D | PASS | `WonderFoodAiSkillContracts.kt` implements `PantryNormalizeSkillContract`; `WonderFoodAiSkillContractsTest.pantryNormalizePreservesOriginalAndAddsConfidenceLabel` and malformed/unsafe coverage passed. Release-facing exact wire naming still belongs to docs/release rows. |
| D02 | `wonderfood-cart-builder` implemented | 8 | D | PASS | `CartBuilderSkillContract.suggestFromPlan` exists and `WonderFoodAiSkillContractsTest.cartBuilderCreatesSuggestionsWithoutDeletingExistingCart` plus missing-context clarification test passed. |
| D03 | `wonderfood-receipt-parse` implemented | 8 | D | PASS | `ReceiptParseSkillContract` exists; tests passed for deterministic known receipt text, offline/provider failure blocked state, and unsafe lines staying review-only. |
| D04 | `wonderfood-recipe-import` implemented | 8 | D | PASS | `RecipeImportSkillContract` exists; tests passed for Samsung Food shared-link proposal, malformed blank input clarification, provider-unavailable fail-closed behavior, and unsafe request blocking. |
| D05 | `wonderfood-meal-plan` implemented | 8 | D | PASS | `MealPlanSkillContract.propose` exists; tests passed for reviewable household plan/share output and hard-allergy exclusion fail-closed behavior. |
| D06 | `wonderfood-nutrition-estimate` implemented | 8 | D | PASS | `NutritionEstimateSkillContract` exists; tests passed for safe reviewable nutrition estimates, ambiguous/empty clarification, and offline/provider-unavailable blocked behavior. |
| D07 | `wonderfood-cooking-coach` implemented | 8 | D | PASS | `CookingCoachSkillContract.nextStep` exists; tests passed for proposal-only coaching and clarification when recipe steps are missing. |
| D08 | Every skill has versioned typed I/O, provenance, confidence, warnings, proposal intent, deterministic-first processing | 8 | D | PASS | `AiSkillEnvelope`, `AiSkillContractMeta`, `AiSkillProvenance`, and `AiSkillProposalIntent` are shared by all contracts; `typedContractIntentsMatchProposalSurfaces` and `typedContractsPropagateMetadataAndProposalOnlyBehavior` passed. |
| D09 | Skills never persist directly; accepted proposals use canonical commands and match equivalent manual/shared input | 8 | D/A | PASS | `WonderFoodAiSkillContractsTest` verifies proposal-only warnings/metadata; `CommandEnvelopeDraftMapperTest` verifies imported envelopes stay review drafts; `HouseholdDraftCommandMapperTest.externalRecipeProposalUsesSameCanonicalCommandSurfaceAsManualDraft` passed and proves accepted external proposals become canonical commands through the same draft mapper as equivalent manual input. Direct-write scan found no AI skill repository/DAO writes, only proposal warning text and unrelated `LiteLlmSettings.save`. |
| D10 | Schema.org Recipe/HowToStep/NutritionInformation/Product/Offer/Organization mapping exists only at boundaries | 8 | D | PASS | Added `SchemaOrgBoundaryMapper` in `app/ai` boundary code and wired recipe import to keep Schema.org data in `RecipeImportProposal.schemaOrgBoundary`; `recipeImportMapsSchemaOrgOnlyAtAiBoundary` passed for Recipe, HowToStep, NutritionInformation, Product, Offer, and Organization. Scan of core model/data/engine plus app data/sync found no Schema.org boundary names. |
| D11 | Golden fixtures cover normal, ambiguous, malformed, unsafe, offline, provider failure; allergy/hard exclusions fail closed; uncertainty and provider/license/privacy remain visible | 8 | D | PASS | `WonderFoodAiSkillContractsTest` passed and covers normal proposals, ambiguous/missing input clarification, malformed/unsafe filtering/blocking, offline/provider failure blocked states, allergy hard-exclusion fail-closed behavior, confidence/provenance/warnings, and proposal-only/privacy policy metadata. |

## E. Product and Release Proof

| ID | Requirement | Original phase | Owner | Status | Evidence |
|---|---|---:|---|---|---|
| E01 | Original architecture acceptance matrix is fully evidenced | 9 | Coordinator | PASS | All 76 rows in this matrix are now `PASS`, including final CI, PR merge, signed APK, release, checksum, and install artifact proof. |
| E02 | Focused model/repository/command tests pass | 9 | A | PASS | Verified `./gradlew :core:model:test :core:engine:test :core:data:testDebugUnitTest --tests "com.wonderfood.core.data.room.RoomHouseholdRepositoryTest" --tests "com.wonderfood.core.data.room.WonderFoodMigrationsTest"` and mapper/spending focused app tests: BUILD SUCCESSFUL. |
| E03 | Focused sync/provider/secret tests pass | 9 | C | PASS | Verified `./gradlew :core:data:testDebugUnitTest --tests "com.wonderfood.core.data.backend.PostgresConnectionParserTest" :app:testFossDebugUnitTest --tests "com.wonderfood.app.sync.DataHomeAdapterTest" --tests "com.wonderfood.app.sync.PostgresGatewayTest" --tests "com.wonderfood.app.sync.CredentialSecretCodecTest" --tests "com.wonderfood.app.sync.GoogleSheetsWorkspaceDraftImporterTest" --tests "com.wonderfood.app.sync.GoogleSheetsGatewayTest" --tests "com.wonderfood.app.sync.NotionGatewayTest" --tests "com.wonderfood.app.sync.WonderFoodWorkspaceSeedFixtureTest" --tests "com.wonderfood.app.sync.WonderFoodWorkspaceSnapshotMergerTest"`: BUILD SUCCESSFUL. |
| E04 | Live Notion, Sheets, and Postgres round trips pass | 9 | C | PASS | Verified live provider round trips: `scripts/quality/run-notion-live-proof.sh` passed with an accessible Notion page parent; `scripts/quality/run-provider-live-proofs.sh sheets` passed against the configured Google Sheet; `scripts/quality/run-local-postgres-live-proof.sh` passed against a local Homebrew Postgres-backed WonderFood HTTP API. Evidence artifacts are written under `build/evidence/live-workspace`. |
| E05 | Focused skill/import/parity/safety tests pass | 9 | D/A | PASS | Verified `./gradlew :app:testFossDebugUnitTest --tests "com.wonderfood.app.ai.WonderFoodAiSkillContractsTest" --tests "com.wonderfood.app.ai.CommandEnvelopeDraftMapperTest" --tests "com.wonderfood.app.data.HouseholdDraftCommandMapperTest" :core:ai:test --tests "com.wonderfood.core.ai.StructuredProposalGatewayTest"`: BUILD SUCCESSFUL. |
| E06 | Full local Android quality harness passes | 9 | Coordinator | PASS | The full local harness completed all unit suites and FOSS/Play app/test-APK assemblies after updating two stale V3 test fixtures to the canonical V4 contract. Its Java 20 daemon crashed during lint; the complete lint set was rerun on Java 17 after replacing seven API-34-only `LocalDate.ofInstant` calls with API-26-safe `Instant.atZone(...).toLocalDate()`. Focused projection tests and `:app:lintFossDebug :app:lintPlayDebug :core:data:lintDebug` then passed. Connected proof remains separate under `E07/E09`. |
| E07 | Connected suites pass on supported low and current API levels | 9 | Coordinator | PASS | Supported emulator proof is green at API 34 and current API 36. API 36 command `ANDROID_SERIAL=emulator-5554 JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew --no-daemon :app:connectedFossDebugAndroidTest :app:connectedPlayDebugAndroidTest :core:data:connectedDebugAndroidTest` completed `BUILD SUCCESSFUL in 3m 21s`: FOSS 41/41, Play 41/41, core:data 3/3, total 85/85 with zero failures/errors/skips. XML evidence is under `app/build/outputs/androidTest-results/connected/debug/flavors/{foss,play}/` and `core/data/build/outputs/androidTest-results/connected/debug/`. API 34 previously passed the same 41/41, 41/41, and 3/3 suites. |
| E08 | Required visual states are screenshot-reviewed without overlap or accessibility regressions | 9 | B/Coordinator | PASS | Direct screenshot review completed for representative empty, populated, validation-error, conflict, light, dark, large-font, landscape, and tablet states. `error-state-before-connect.png` visibly reports `Paste a Google Sheet link first.` without overlap; the remaining selected captures are recorded in `visual-proof-b13-e08/visual_matrix.md`. |
| E09 | Release candidate installs, launches, restarts, and preserves canonical state on physical phone | 9 | Coordinator | PASS | Verified on physical Samsung `SM-S918U1` / S23 Ultra / API 36. Streamed install of `app-foss-debug.apk` succeeded; package metadata reports `1.0.5-foss`, `versionCode=5`, and `targetSdk=36`. The app launched, selected Local, reached the Now screen, was force-stopped, and relaunched directly to the same shell. `wonderfood-v105-household.db` was pulled before and after restart; both SHA-256 values are `2bb28c173aea4538c5a795bcea19d8e1ca39619621d61b340d19e2013801657f` and byte comparison returned `0`. Screenshots, UI XML, package metadata, APK checksum, database copies, and install logs are under `physical-device-e09-20260720-200653/README.md`. |
| E10 | README, FEATURES, CHANGELOG, architecture, privacy, setup, and release notes match verified behavior | 9 | D | PASS | Updated stale testing/quality docs that still named `FoodChatStore` as runtime authority or transaction proof. Verified docs claim scan across README, FEATURES, CHANGELOG, docs, and production app sources; remaining hits are Postgres raw-credential warning copy, historical `FoodMemory` rename note, generic design/foundation wording, or Compose imports, not active release overclaims. Follow-up release gates added in `task_plan.md`: app-visible terminology/i18n readiness must be reviewed so internal architecture terms such as "canonical cart" do not ship in normal UI, and every changed UI/DB/Notion/Sheets/Postgres/AI workflow must map to deterministic local/fake tests before relying on live or device proof. `git diff --check`: clean. |
| E11 | Docs do not call providers foundation/dump/scaffold/partial sync and do not overclaim missing proof | 9 | D | PASS | Updated `README.md`, `FEATURES.md`, `CHANGELOG.md`, and `docs/testing/README.md` to remove active Supabase/direct-DSN wording, provider "foundation"/dump/scaffold claims, legacy import/export wording, and final 1.0.5 release-proof overclaims. Verified docs claim scans; remaining hits are policy/prohibition lines for raw database credentials/socket paths or generic non-provider design/history phrases, not provider dump/scaffold/partial-sync claims. `git diff --check`: clean. |
| E12 | CI passes on final release commit | 9 | Coordinator | PASS | Final release commit is `9a132f6d738d4aa14a7fcf844d688018abcfc489` on `main`. PR #49 CI run `29794134108` passed `local-quality`, `device-quality (26)`, and `device-quality (35)`. Android Release run `29794459076` also passed on the same commit. |
| E13 | Signed Play and FOSS APKs and SHA-256 checksums are produced | 9 | Coordinator | PASS | Android Release run `29794459076` passed `Validate tag and signing secrets`, `Build signed release APKs`, and `Verify and package APKs`. Published assets include `WonderFood-foss-v1.0.5.apk`, `WonderFood-play-v1.0.5.apk`, and `SHA256SUMS.txt`; downloaded artifacts verified with `shasum -a 256 -c SHA256SUMS.txt`: both APKs `OK`. |
| E14 | One final PR is created, reviewed, and merged | 9 | Coordinator | PASS | Final release-history fix PR #49 (`codex/release-105-changelog` -> `main`) passed required checks and was squash-merged. Earlier release implementation PRs #47 and #48 are also merged into the final `v1.0.5` tag history. |
| E15 | v1.0.5 tag/release is published with APKs, checksums, notes, workflow evidence | 9 | Coordinator | PASS | Annotated tag `v1.0.5` was moved to final commit `9a132f6d738d4aa14a7fcf844d688018abcfc489` and pushed. GitHub Release `v1.0.5` was published at `https://github.com/vaddisrinivas/wonderfood/releases/tag/v1.0.5` by Android Release run `29794459076` with FOSS APK, Play APK, checksum file, and release notes. |
| E16 | Install URL/artifact is verified and reported | 9 | Coordinator | PASS | Verified release assets are downloadable from `https://github.com/vaddisrinivas/wonderfood/releases/tag/v1.0.5`. Local download to `/tmp/wf-v105-release-1784599362` and checksum verification reported `WonderFood-foss-v1.0.5.apk: OK` and `WonderFood-play-v1.0.5.apk: OK`. |
| E17 | No bank credentials/financial account data, provider secrets, raw snapshots, or private prompts leak | 3,5-9 | All | PASS | Secret-shaped production/docs scan returned no matches for AWS/OpenAI-style keys, Notion `secret_...`, raw Postgres/JDBC URLs, password parameters, service-role assignments, API-key assignments, or private-prompt assignments. Policy scan hits are warning copy only: README/FEATURES/CHANGELOG/MainScreen forbid raw database credentials, privileged server tokens, raw socket paths, or direct PostgreSQL. Existing adapter tests prove provider failure summaries redact Google Sheets, Notion, and Postgres tokens. |

## Stop Conditions

Do not mark complete while any row is not `PASS`, or while any of these remains true:

- Production uses the legacy runtime.
- Local mode requires network, AI, account, or provider.
- A provider is one-way, append-only, or snapshot-dump based.
- Notion/Sheets proof lacks visual standalone inspection.
- Postgres permits cross-household access or requires service-role credentials, DB password, direct DSN, or raw database socket access on Android.
- AI mutates without an editable proposal and canonical validation.
- Emulator, physical device, live provider, CI, signing, docs, PR merge, tag, release, checksum, or install proof is missing.
